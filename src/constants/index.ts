export enum Roles {
    ADMIN = 'admin',
    MONITOR = 'monitor',
    USER = 'user',
}

export enum UserStatus {
    ACTIVE = 'active',
    BANNED = 'ban',
}

export enum Languages {
    EN = 'en',
    HI = 'hi',
    // Add more as needed
}

export enum ContentType {
    FRAME = 'frame',
    DIALOGUE = 'dialogue',
    EYES = 'eyes',
}

export enum ContentStatus {
    PENDING = 'pending',
    APPROVED = 'approved',
    REJECTED = 'rejected',
}
